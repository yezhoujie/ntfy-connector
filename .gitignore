__pycache__/
*.pyc
.DS_Store
.venv/

# 项目本地 Claude 说明，只在本机（用户定）
CLAUDE.md

# 任务过程文档：需求 / 设计 / 任务拆分 / 状态 / 派发说明 / worker 报告
# 只在本机保留，不入仓
task/

# agent-ntfy 远程交互模式的项目级运行时状态
.agent-ntfy/

# 编辑器本地配置
.vscode/
